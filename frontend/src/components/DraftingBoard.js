import React from 'react';

const Crafts = ({ title, img_url }) => (
  <div className="craft">
    {/* <img src={ img_url } alt={title}/>
    <h3>{ title }</h3> */}
    <p>Test</p>
  </div>
)

export default Crafts; 